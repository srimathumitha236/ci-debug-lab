name: CI Debug

on:
  push:
    branches: [ main ]

jobs:
  build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Install package
        run: npm install

      - name: Run test
        run: npm test
